#lang racket/gui

(require "../style-config.rkt")
(require "../atomic/text-field.rkt")
(require "../atomic/text-area.rkt")

;; Label + TextField / TextArea 组合控件
;; 目前为空实现，后续会完善

;; 暂时不导出，等实现完成后再导出
;; (provide input-field%)
